public class AccountKeyValue {
    public void AccountNameKey(){
        Map<list<Account>,id> macc=new Map<list<Account>,id>();
       list<Account> alist=[Select Id,Name from Account];
        for(Account a:alist){
            //macc.add(a.Name,a.id);
        }
    }

}